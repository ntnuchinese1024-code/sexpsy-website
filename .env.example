# SofaCounter（沙發計數器）用的 Abacus namespace。
#
# 刻意不要用網站真實網域名稱（例如 sexpsy.tw）當 namespace：
# 這個 repo 是公開的，字面字串會被公開掃描/爬取，導致計數器被外部流量
# （bot／其他無關的人）任意打 /hit 灌水。改用一組不可猜測的隨機字串
# （例如 UUID），可以大幅降低被公開掃描到的機率。
#
# 產生新值：
#   node -e "console.log(crypto.randomUUID())"
#
# 本機開發：複製這個檔案為 .env，填入實際值（.env 已加進 .gitignore，不會被 commit）。
# GitHub Actions 部署：在 repo 的 Settings → Secrets and variables → Actions
# 新增一個 secret，名稱 SOFA_NAMESPACE，值就是你產生的 UUID
#（.github/workflows/deploy.yml 會在 build 時把它注入成 PUBLIC_SOFA_NAMESPACE）。
PUBLIC_SOFA_NAMESPACE=

# scripts/generate-article-images.mjs（文章封面圖生成）目前改成純本機
# SVG＋sharp 生成，不需要任何 API key。如果之後想接回付費 AI 生圖 API，
# 可以從 git commit 19780d6（feat: 生圖 API 從 OpenAI DALL-E 3 改成
# Google Gemini）把 GEMINI_API_KEY 那段設定跟呼叫邏輯拿回來。
