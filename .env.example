# SofaCounter（沙發計數器）用的 Abacus namespace。
#
# 刻意不要用網站真實網域名稱（例如 sexpsy.tw）當 namespace：
# 這個 repo 是公開的，字面字串會被公開掃描/爬取，導致計數器被外部流量
# （bot／其他無關的人）任意打 /hit 灌水。改用一組不可猜測的隨機字串
# （例如 UUID），可以大幅降低被公開掃描到的機率。
#
# 產生新值：
#   node -e "console.log(crypto.randomUUID())"
#
# 本機開發：複製這個檔案為 .env，填入實際值（.env 已加進 .gitignore，不會被 commit）。
# GitHub Actions 部署：在 repo 的 Settings → Secrets and variables → Actions
# 新增一個 secret，名稱 SOFA_NAMESPACE，值就是你產生的 UUID
#（.github/workflows/deploy.yml 會在 build 時把它注入成 PUBLIC_SOFA_NAMESPACE）。
PUBLIC_SOFA_NAMESPACE=

# --------------------------------------------------------------------------
# OpenAI API Key，給 scripts/generate-article-images.mjs（AI 封面/插圖生成）用。
#
# ⚠️ 這組跟上面的 PUBLIC_SOFA_NAMESPACE 不一樣，是真正的機密資訊：
#   - 絕對不要加 PUBLIC_ 前綴（那個前綴會被 Vite 打包進瀏覽器看得到的網站原始碼）
#   - 絕對不要 commit 進 git、絕對不要貼到 issue/對話/截圖裡
#   - 這支腳本只在本機用 `node --env-file=.env` 執行，key 不會出現在網站上
#   - 如果不小心外洩（commit 進 git、貼在公開的地方等），立刻到
#     https://platform.openai.com/api-keys revoke 舊 key、產生新的，
#     並確認舊 key 沒有殘留在 git 歷史裡
#
# 申請：https://platform.openai.com/api-keys（需要綁定付款方式）
# 費用：DALL-E 3 standard quality 約 US$0.04（1024x1024）／US$0.08（1792x1024）
#       一張，是計費 API，執行腳本前請先看 --dry-run 印出來的預估花費
OPENAI_API_KEY=
