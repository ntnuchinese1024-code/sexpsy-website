

1. **新建文章**：在 Obsidian 按快捷鍵 `Cmd + N` 建立新筆記（預設會自動存進 `src/content/articles/`）。
    
2. **插入模板**：按 `Cmd + P`，輸入 `Insert template` 並按下 Enter，選擇「文章模板」，填寫標題與摘要。
    
3. **寫作與貼圖**：直接寫內容，圖片直接拖進筆記裡即可。
    
4. **一鍵上架**：寫完後按 `Cmd + P`，輸入 `Commit-and-sync` 並按下 Enter！

